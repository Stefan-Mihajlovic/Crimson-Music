import type { PlayerDetailsTab } from '@/components/player-details-tabs.types';

// Native players continue to use the existing gesture-driven presentation.
export default function DesktopPlayer(_props: { initialTab?: PlayerDetailsTab }) {
  return null;
}
